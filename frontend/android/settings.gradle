pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven { url 'https://registry.npmjs.org/@capacitor/android/maven' }
    }
}

rootProject.name = 'PotBot'

include ':app'