// File: pot bot-main/frontend/android/settings.gradle

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    // Allow project modules to declare their own repositories
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)

    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'PotBot'
include ':app'
include ':capacitor-cordova-android-plugins'
project(':capacitor-cordova-android-plugins').projectDir = new File('capacitor-cordova-android-plugins')

// If you’re using the local-engine approach instead of Maven Central, also include:
// include ':capacitor-android'
// project(':capacitor-android').projectDir = file('../node_modules/@capacitor/android/android')

apply from: 'capacitor.settings.gradle'