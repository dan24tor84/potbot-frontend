buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://registry.npmjs.org/' }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://registry.npmjs.org/' }
    }
}