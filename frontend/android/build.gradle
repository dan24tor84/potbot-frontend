// File: frontend/android/build.gradle

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.4.0'
        classpath 'com.google.gms:google-services:4.4.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()

        // ✅ REQUIRED for Capacitor 5 plugins to resolve
        maven { url 'https://plugins.capacitorjs.com/' }
    }
}
